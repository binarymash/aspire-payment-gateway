<Project>
  <PropertyGroup>
    <!-- define AWS to use Dynamo local, or AZURE to use CosmosDb emulator.  -->
    <!-- If neither is set then an in-memory store will be used  -->
    <DefineConstants>AWS;$(DefineConstants)</DefineConstants>
  </PropertyGroup>
</Project>